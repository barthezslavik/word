<%= form_with(model: entity, local: true) do |form| %>
  <% if entity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(entity.errors.count, "error") %> prohibited this entity from being saved:</h2>

      <ul>
      <% entity.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :english %>
    <%= form.text_field :english %>
  </div>

  <div class="field">
    <%= form.label :article %>
    <%= form.text_field :article %>
  </div>

  <div class="field">
    <%= form.label :german %>
    <%= form.text_field :german %>
  </div>

  <div class="field">
    <%= form.label :done_article %>
    <%= form.check_box :done_article %>
  </div>

  <div class="field">
    <%= form.label :done_german %>
    <%= form.check_box :done_german %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
